<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="Author" content="Vyacheslav Odinokov">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title>Cloud MU Online</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <article class="root">
      <% if user_signed_in? %>
        <header>
          <% if player %>
            <div class="player-info">
              <p class="player-status">
                <span class="icon dark-wizard-icon"></span>
                <%= player.character.name %>
              </p>

              <p class="player-status">
                <span class="icon health-icon"></span>
                100
                <span class="icon mana-icon"></span>
                100
              </p>

              <p class="player-status">
                <span class="icon gold-coin-icon"></span>
                228
              </p>
            </div>
          <% end %>
        </header>
      <% end %>

      <main>
        <% if notice or alert %>
          <article>
            <p><%= notice %></p>
            <p><%= alert %></p>
          </article>
        <% end %>

        <%= yield %>

        <% if user_signed_in? %>
          <section class="">
            <div class="hr"></div>
            <div class="hr"></div>
            <% if player %>
              <ul class="main-menu player-menu">
                <li class="main-menu-item"><%= link_to "Character", character_path(player.character) %></li>
                <li class="main-menu-item"><%= link_to "Main page", root_path %></li>
              </ul>
            <% end %>
            <div class="hr"></div>
            <div class="hr"></div>
          <section>
        <% end %>
      </main>

      <footer>
        <small>&copy; 2024 Vyacheslav Odinokov. All rights reserved.</small>
      </footer>
    </article>
  </body>
</html>
